query findServer($serverUuid: String) {
    server(uuid: $serverUuid) {
        ...ServerFields
    }
}

query findServers {
    servers {
        ...ServerFields
    }
}

fragment ServerFields on Server {
    uuid
    serverName
}

type Mutation {
    createServer(uuid: String, serverName: String): Server
}
